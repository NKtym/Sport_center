### Всего три топика:
- connect
- simple
- streams
### Сообщения посланные скриптом пишем в топик simple, взятые из БД в connect, из kafka streams - в streams
### Формат четырёх событий в .json файлах
### producer.py пишет в топик simple сообщения с ключом entityId (id абонемента) - сохраняем все посещения с одним абонементом в одну партицию, сохраняем последовательность обработки событий