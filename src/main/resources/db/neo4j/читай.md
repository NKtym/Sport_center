shell:
docker compose exec neo4j cypher-shell -u neo4j -p '1234554321'

Для создания пользователей: 
CREATE USER reader_user SET PLAINTEXT PASSWORD 'readerpass' CHANGE NOT REQUIRED;
CREATE USER publisher_user SET PLAINTEXT PASSWORD 'publisherpass' CHANGE NOT REQUIRED;

GRANT ROLE reader TO reader_user;
GRANT ROLE publisher TO publisher_user;

Проверка: Выполнить как админ
CREATE (a:User {id: 'u-test-1', name: 'Test User 1'});
CREATE (p:Product {id: 'p-test-1', category: 'test'});

docker compose exec neo4j cypher-shell -u reader_user -p 'readerpass'
CREATE (x:Test {id:'should_not_create'}) RETURN x;

docker compose exec neo4j cypher-shell -u publisher_user -p 'publisherpass'
MATCH (u:User) RETURN u.id, u.name LIMIT 5;