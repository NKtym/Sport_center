Чтобы сделать нормальную репликацию выполни это

docker exec -it mongo mongosh

rs.reconfig({
  _id: "rs0",
  members: [
    { _id: 0, host: "mongo:27017", priority: 2 },
    { _id: 1, host: "mongo2:27017", priority: 1 },
    { _id: 2, host: "mongo3:27017", priority: 0 }
  ]
}, { force: true })

Если необходимо загенерить данные много то -> docker exec -i mongo mongosh sports_center < mongo_seed.js
